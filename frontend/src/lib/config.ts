/** Centralized public links and visual assets used across the frontend. */
export const HERO_BACKGROUND_URL =
  "https://customer-assets-wrfwihn1.emergentagent.net/job_force-wellness/artifacts/wxfio8hr_01-homepage-hero.png";

/**
 * Set this once a real repository URL is available. The source button remains
 * hidden while this value is empty so the interface never invents a link.
 */
export const GITHUB_REPOSITORY_URL = "";